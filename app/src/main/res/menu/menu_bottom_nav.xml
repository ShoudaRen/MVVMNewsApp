<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
    android:id="@+id/breaking_icon"
    android:icon="@drawable/ic_breaking"
    android:title="@string/breaking"></item>
    <item
        android:id="@+id/search_icon"
        android:icon="@drawable/ic_search"
        android:title="@string/search"></item>
    <item
        android:id="@+id/bookmarks_icon"
        android:icon="@drawable/ic_bookmarks"
        android:title="@string/bookmarks"></item>

</menu>